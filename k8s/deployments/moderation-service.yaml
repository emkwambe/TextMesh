# =================================
# MODERATION SERVICE DEPLOYMENT
# =================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-service
  namespace: textmesh
  labels:
    app: textmesh
    component: moderation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: textmesh
      component: moderation-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: textmesh
        component: moderation-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3007"
    spec:
      serviceAccountName: textmesh-service-account
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: moderation-service
          image: textmesh/moderation-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3007
              protocol: TCP
          envFrom:
            - configMapRef:
                name: textmesh-config
            - secretRef:
                name: textmesh-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 5
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
